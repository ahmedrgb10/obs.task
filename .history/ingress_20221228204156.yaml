apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: basicIngress
spec:
  rules:
    - host: obs.task.com
      http:
        paths:
          - path: /
            backend:
              serviceName: juice
              servicePort: 3000